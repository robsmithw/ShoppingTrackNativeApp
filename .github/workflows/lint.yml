name: lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: node:latest
    steps:
      - run: yarn
      - run: yarn lint

  lint_code_formatter:
    runs-on: node:latest
    steps:
      - run: yarn
      - run: yarn code-formatter-check
