name: lint

on: [push, pull_request]

jobs:
  lint:
    container: node
    steps:
      - run: yarn
      - run: yarn lint

  lint_code_formatter:
    container: node
    steps:
      - run: yarn
      - run: yarn code-formatter-check
